<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.stenzek.duckstation">

<!--    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />-->
    <uses-permission android:name="android.permission.VIBRATE" />
<!--    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Final Fantasy VII"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:hasFragileUserData="true">

        <activity
            android:name=".MemoryCardEditorActivity"
            android:label="@string/title_activity_memory_card_editor"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".GameDirectoriesActivity"
            android:label="@string/title_activity_game_directories"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- ================= EmulationActivity ================= -->

        <activity
            android:name=".EmulationActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:exported="true"
            android:immersive="true"
            android:label="@string/title_activity_emulation"
            android:parentActivityName=".SingleGameActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="sensorLandscape">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.github.stenzek.duckstation.SingleGameActivity" />

            <!-- ORIGINAL:
            android:parentActivityName=".MainActivity"
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.github.stenzek.duckstation.MainActivity" />
            -->
        </activity>

        <!-- ================= Other Activities (Unchanged) ================= -->

        <activity
            android:name=".SettingsActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.github.stenzek.duckstation.MainActivity" />
        </activity>

        <activity
            android:name=".ControllerSettingsActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/controller_mapping_activity_title"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.github.stenzek.duckstation.MainActivity" />
        </activity>

        <activity
            android:name=".GamePropertiesActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/activity_game_properties"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.github.stenzek.duckstation.MainActivity" />
        </activity>

        <!-- ================= SingleGameActivity (NEW LAUNCHER) ================= -->

        <activity
            android:name=".SingleGameActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="sensorLandscape">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ================= MainActivity (Launcher removed) ================= -->

        <!-- ORIGINAL LAUNCHER BLOCK:
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>
